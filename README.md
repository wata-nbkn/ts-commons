# ts-commons

This is a utility module project for TypeScript.

## For development

### Commands

- test: `$ npm run test`

- build: `$ npm run build`
  - The built files will be distributed to `dit` folder.
